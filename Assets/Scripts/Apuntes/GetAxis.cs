using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MovimientoGetAxis : MonoBehaviour
{/*
    Rigidbody rbd;
    Vector3 gravity = new Vector3(0, -9.81f, 0);
    public float EscalaGravedad;

    // Start is called before the first frame update
    void Start()
    {
        Physics.gravity = gravity * EscalaGravedad;
    }

    // Update is called once per frame
    void Update()
    {
        //Inpu0t.GetAxis("Horizontal");
        //rbd.velocity = Vector3.ClampMagnitude(rbd.velocity, 10);
    }

    private void OnCollisionEnter(Collision col)
    {
        col.gameObject.name;
        col.gameObject.CompareTag("Tag");
    }

    private void OnTriggerEnter(Collider other)
    {
        other.CompareTag("Tag");

        Destroy(other.gameObject);
        Time.time;
        Time.timeScale;
    }
    */
}
